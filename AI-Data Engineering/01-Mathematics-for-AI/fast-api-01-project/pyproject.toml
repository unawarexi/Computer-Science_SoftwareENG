[project]
name = "fast-api-01-project"
version = "0.1.0"
description = "FastAPI project for 01-Mathematics-for-AI"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic-settings"
]

[tool.uv]
managed = true
