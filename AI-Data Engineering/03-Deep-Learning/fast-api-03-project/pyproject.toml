[project]
name = "fast-api-03-project"
version = "0.1.0"
description = "FastAPI project for 03-Deep-Learning"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic-settings"
]

[tool.uv]
managed = true
