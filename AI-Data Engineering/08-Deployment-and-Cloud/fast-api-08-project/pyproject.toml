[project]
name = "fast-api-08-project"
version = "0.1.0"
description = "FastAPI project for 08-Deployment-and-Cloud"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic-settings"
]

[tool.uv]
managed = true
