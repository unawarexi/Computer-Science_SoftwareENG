# Interrupts and Traps

- **Interrupt:** Signal from hardware or software to the CPU, causing it to stop current execution and handle an event (e.g., I/O completion).
- **Trap:** Synchronous interrupt caused by an error or specific instruction (e.g., divide by zero, system call).

Interrupts and traps enable responsive and robust OS behavior.
