# Block vs Character Devices

- **Block Devices:** Transfer data in fixed-size blocks (e.g., hard drives, SSDs). Support random access.
- **Character Devices:** Transfer data one character at a time (e.g., keyboards, serial ports). Support sequential access.

The OS uses different interfaces and buffering strategies for each type.
