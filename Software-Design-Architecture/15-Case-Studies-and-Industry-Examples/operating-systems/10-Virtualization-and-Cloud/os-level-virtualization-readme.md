# OS-Level Virtualization

OS-level virtualization allows multiple isolated user-space instances (containers) to run on a single OS kernel.

- **Examples:** Docker, LXC, OpenVZ.
- **Benefits:** Efficient resource usage, fast startup, easy deployment.
- **Limitations:** All containers share the same kernel; less isolation than VMs.
