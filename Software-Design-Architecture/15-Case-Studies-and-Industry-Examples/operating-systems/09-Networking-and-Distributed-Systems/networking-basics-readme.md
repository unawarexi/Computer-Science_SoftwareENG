# Networking Basics (Protocols, Sockets, Ports)

- **Protocols:** Rules for data exchange (TCP, UDP, IP, HTTP, FTP, etc.).
- **Sockets:** Endpoints for sending/receiving data across a network.
- **Ports:** Logical channels for network services (e.g., HTTP uses port 80).

Networking enables communication between computers and devices over local and wide area networks.
