/**
 * this is a local module
 * they're the modules we create by ourselves in our project
 * every file in node js is a local module
 * @param {*} myName 
 */

function callName(myName) {
    console.log(`hello call ${myName}`)
}

callName("andrew developer")